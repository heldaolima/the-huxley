#include <stdio.h>

int main()
{
    int reaistem, weekday, diasatraso, valorantes, base, ssalario;
    
    multa = valorantes + ((diasatraso - weekday) * base);
    

    // analisando a questao dos dias de semana
    if (weekday == 1){
        if (diasatraso > )
    }
    return 0;
}