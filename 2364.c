#include <stdio.h>

#define OBJETIVO 10000000

int golpe(int valor)
{
    if ()
}
int main()
{

    int valor, result;
    scanf("%d", &valor);
    result = golpe(valor, 1);
    printf("%d\n", result);
    //M é o valor básico de um ingresso
    //x é a quantidade de pessoas que o pagaram
    //10 milhões de dólares ou mais
    return 0;
}